{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.AndyAnh174/vibe-hnindex",
  "description": "Index source code into a local knowledge base, search with keyword + semantic + hybrid modes. Supports streaming search, fuzzy matching, regex, symbol filters, and parallel indexing.",
  "repository": {
    "url": "https://github.com/AndyAnh174/vibe-hnindex",
    "source": "github"
  },
  "version": "0.12.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "vibe-hnindex",
      "version": "0.12.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "Ollama embedding server URL",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OLLAMA_URL"
        },
        {
          "description": "Ollama embedding model name (default: bge-m3:567m)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OLLAMA_MODEL"
        },
        {
          "description": "Embedding vector size — must match Ollama output for OLLAMA_MODEL (default: 1024)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "EMBEDDING_DIMENSIONS"
        },
        {
          "description": "Qdrant vector database URL",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "QDRANT_URL"
        },
        {
          "description": "Qdrant API key (Qdrant Cloud or API-key-protected clusters)",
          "isRequired": false,
          "format": "string",
          "isSecret": true,
          "name": "QDRANT_API_KEY"
        },
        {
          "description": "SQLite database storage path (default: ~/.vibe-hnindex)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "STORAGE_PATH"
        },
        {
          "description": "Number of worker threads for parallel indexing (default: auto = CPU-1). Set to 1 for single-threaded",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "INDEX_WORKERS"
        },
        {
          "description": "Files per worker batch during parallel indexing (default: 8)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "INDEX_PARALLEL_BATCH"
        },
        {
          "description": "When true, omitting search mode uses auto (heuristic keyword/hybrid) — v0.5.0",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_AUTO_ROUTE"
        },
        {
          "description": "When not false, keyword search with zero hits runs semantic fallback (default on)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_KEYWORD_FALLBACK_SEMANTIC"
        },
        {
          "description": "Enable streaming search by default — parallel keyword+semantic, progress, preview (v0.9.0)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_STREAM_ENABLED"
        },
        {
          "description": "Enable fuzzy search re-ranking for all searches (v0.8.1)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_FUZZY_ENABLED"
        },
        {
          "description": "Max cache entries for search results LRU (default: 100)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_CACHE_SIZE"
        },
        {
          "description": "Cache TTL in milliseconds (default: 300000 = 5 min)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_CACHE_TTL_MS"
        },
        {
          "description": "Set to false to disable post-retrieval reorder (v0.6.0)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_RERANK"
        },
        {
          "description": "Max distinct results in rerank pool before trimming to limit (default: 50)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_RERANK_POOL"
        },
        {
          "description": "POST URL for external reranker (optional)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "RERANK_URL"
        },
        {
          "description": "Timeout for RERANK_URL request in ms (default: 15000)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "RERANK_TIMEOUT_MS"
        },
        {
          "description": "Overall timeout for search operations in ms (default: 60000)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "SEARCH_TIMEOUT_MS"
        },
        {
          "description": "Timeout for Ollama API calls in ms (default: 30000)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OLLAMA_TIMEOUT_MS"
        },
        {
          "description": "Timeout for Qdrant API calls in ms (default: 15000)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "QDRANT_TIMEOUT_MS"
        },
        {
          "description": "Enable chat memory — auto-track tool calls, save/load/ingest context (v0.12.0)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "CHAT_MEMORY_ENABLED"
        },
        {
          "description": "Max entries in chat_context load (default: 20)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "CHAT_MEMORY_LOAD_LIMIT"
        },
        {
          "description": "Max age in hours for context retrieval (default: 168 = 7 days)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "CHAT_MEMORY_MAX_AGE_HOURS"
        },
        {
          "description": "Thread TTL in ms — reuse latest thread if within this window (default: 3600000 = 1h)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "CHAT_MEMORY_THREAD_TTL_MS"
        },
        {
          "description": "Enable Qdrant vector storage for chat context — enables semantic search on load (default: true)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "CHAT_MEMORY_VECTOR_ENABLED"
        }
      ]
    }
  ]
}


