# Benchmark fixture

Tiny codebase used by `scripts/mcp-search-benchmark.mjs` to compare embedding models (index + search modes).

- TypeScript: `src/embed-service.ts` — symbols `parseEmbeddingDimensions`, `embedBatch`
- Vietnamese notes: `docs/notes-vi.md`
